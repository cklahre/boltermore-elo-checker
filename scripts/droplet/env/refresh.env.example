# Optional: /opt/eloevent/env/refresh.env — sourced by refresh-leaderboard.sh
#
# Pull or rebuild matches BEFORE local-elo runs — used by systemd timer and manual/GitHub refresh.
# Preferred: shards + bcp-matches.manifest committed or rsync/sync-matches-to-droplet; leave unset if so.
#
# Transition / emergency: UPDATE_MATCHES_CMD can still curl a legacy monolith into data/
# UPDATE_MATCHES_CMD='curl -fsSL -o /opt/eloevent/data/bcp-matches.json "https://your-space.nyc3.digitaloceanspaces.com/bcp-matches.json?AWS_KEY=..."'
#
# UPDATE_MATCHES_CMD='cd /root/eloevent && ./bcp-export-matches … -out /opt/eloevent/data/bcp-matches.json'
#
# UPDATE_MATCHES_CMD=
